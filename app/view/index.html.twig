{% extends 'base.html.twig' %}

{% block title %}List{% endblock title %}
{% block content %}
<style type="text/css">
    table {
        border-collapse: collapse;
    }
    table th,
    table td {
        padding: 0 3px;
    }
    table.brd th,
    table.brd td {
        border: 1px solid #000;
    }
</style>
<table class="brd">
    <tr>
        <th>№</th>
        <th>Name</th>
        <th>surname</th>
        <th>sex</th>
        <th>group</th>
        <th>age</th>
        <th>faculty</th>
    </tr>
    {% for student in list %}
    <tr>
        <td> {{ loop.index }}</td>
        <td> {{ student.name }}</td>
        <td> {{ student.surname }}</td>
        <td> {{ student.sex }}</td>
        <td> {{ student.group }}</td>
        <td> {{ student.age }}</td>
        <td> {{ student.faculty }}</td>
        <td>
            <a href="/list/UpdateStudent/{{ student.id }}">
                <i>update</i>
            </a>
        </td>
        <td>
            <a href="/list/delete/{{ student.id }}">
                <i>delete</i>
            </a>
        </td>
    </tr>
    {% endfor %}
    <?php endforeach;?>
</table>
<a href="/list/addNewStudent">
    <i>add new student</i>
</a>
{% endblock content %}